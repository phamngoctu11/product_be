server:
  port: 8080
app:
  public-base-url: ${APP_PUBLIC_BASE_URL:http://localhost:8080}
  rate-limit:
    enabled: ${APP_RATE_LIMIT_ENABLED:true}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200}
chat:
  realtime:
    redis-pubsub:
      enabled: ${CHAT_REALTIME_REDIS_PUBSUB_ENABLED:false}
  migration:
    mysql-to-mongo:
      enabled: ${CHAT_MIGRATION_MYSQL_TO_MONGO_ENABLED:false}
consultation:
  bonus:
    percent: ${CONSULTATION_BONUS_PERCENT:5}
keycloak:
  server-url: ${KEYCLOAK_SERVER_URL:http://localhost:8180}
  realm: ${KEYCLOAK_REALM:my-workflow}
  client-id: ${KEYCLOAK_CLIENT_ID:workflow-frontend}
  admin:
    realm: ${KEYCLOAK_ADMIN_REALM:master}
    client-id: ${KEYCLOAK_ADMIN_CLIENT_ID:admin-cli}
    username: ${KEYCLOAK_ADMIN_USERNAME:admin}
    password: ${KEYCLOAK_ADMIN_PASSWORD:admin}
spring:
  application:
    name: my-workflow-app
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/my-workflow}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:tup20658@gmail.com}
    password: ${MAIL_PASSWORD:eoyi rxsi qquw xmhs}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: ${DB_URL:jdbc:mysql://103.149.170.20:3306/db_A46623}
    username: ${DB_USERNAME:A46623}
    password: ${DB_PASSWORD:A46623@thanglong}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/workflow_chat}
      auto-index-creation: ${MONGODB_AUTO_INDEX_CREATION:false}
    redis:
      # Chạy local sẽ dùng localhost, chạy docker compose sẽ dùng biến REDIS_HOST=redis
      host: ${REDIS_HOST:localhost}
      port: 6379
      timeout: 10000
      fail-on-error: true
  cache:
    type: redis
    redis:
      time-to-live: 3600000
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update

camunda.bpm:
  deployment-resource-pattern:
    - classpath*:**/*.bpmn
  database:
    schema-update: true
  admin-user:
    id: ${CAMUNDA_ADMIN_ID:admin}
    # Đã thêm fallback cho password để không báo lỗi khi khởi chạy local
    password: ${CAMUNDA_ADMIN_PASSWORD:admin}
    first-name: ${CAMUNDA_ADMIN_FIRST_NAME:Admin}
    last-name: ${CAMUNDA_ADMIN_LAST_NAME:User}
    email: ${CAMUNDA_ADMIN_EMAIL:admin@example.com}
  generic-properties:
    enforce-history-time-to-live: false

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dgoolohc8}
  api-key: ${CLOUDINARY_API_KEY:768686576265143}
  api-secret: ${CLOUDINARY_API_SECRET:puDa8eZvm0E3ttWKAzxS6nUDFn8}
momo:
  # Các biến này chưa có trong docker-compose nên tạm thời để trống hoặc bạn tự điền mã partner của mình vào sau dấu :
  partnerCode: ${MOMO_PARTNER_CODE:MOMOBKUN20180529}
  accessKey: ${MOMO_ACCESS_KEY:klm05TvNBzhg7h7j}
  secretKey: ${MOMO_SECRET_KEY:at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa}
  apiUrl: ${MOMO_API_URL:https://test-payment.momo.vn/v2/gateway/api/create}
  redirectUrl: ${MOMO_REDIRECT_URL:http://localhost:4200/payment-success}
  ipnUrl: ${MOMO_IPN_URL:https://pseudoemotional-unrequisitely-jeri.ngrok-free.dev/api/payment/momo-callback}
  mock-enabled: ${MOMO_MOCK_ENABLED:true}
